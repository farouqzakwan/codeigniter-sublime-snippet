<snippet>
	<content><![CDATA[

\$${1:data} = array(
						"name" => "${2:name}", 
						"type" => "${3:text}",
						"id" => "${4:username}",
						"placeholder" => "${5:johnDoe}",
						"size" => "${6:50}"
						);
					echo form_input(\$${1:data});

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>ci-form-input</tabTrigger> 
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.php</scope>
</snippet>
